<local:UpvotePostsBase
    x:Class="QuoraDominatorUI.QDViews.Voting.UpvotePosts"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:DHCustomControl="clr-namespace:DominatorUIUtility.CustomControl;assembly=DominatorUIUtility"
    xmlns:QDcustomControl="clr-namespace:QuoraDominatorUI.CustomControl"
    xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:QuoraDominatorUI.QDViews.Voting"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="UpvotePost"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Name="MainGrid">
        <StackPanel VerticalAlignment="Top">
            <DHCustomControl:HeaderControl
                Name="UpvotePostsHeader"
                CampaignName="{Binding CampaignName}"
                CancelEditCommand="{Binding CancelEditCommand}"
                InfoCommand="{Binding InfoCommand}"
                IsCampaignNameEditable="{Binding IsEditCampaignName}"
                IsCancelEditVisible="{Binding CancelEditVisibility}" />
        </StackPanel>
        <Grid Margin="0,40,0,80" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ScrollViewer>
                <StackPanel>
                    <Expander
                        Margin="5"
                        Header="{DynamicResource LangKeySearchQuery}"
                        IsExpanded="True">
                        <!--  SearchQuery Control  -->
                        <DHCustomControl:SearchQueryControl
                            Name="UpvotePostsSearchControl"
                            AddQueryCommand="{Binding AddQueryCommand}"
                            CommandParameter="{Binding ElementName=UpvotePost}"
                            CustomFilterCommand="{Binding CustomFilterCommand}"
                            IsExpanded="{Binding IsExpanded, ElementName=UpvotePostsHeader, UpdateSourceTrigger=Explicit}"
                            ListQueryInfo="{Binding UpvotePostsModel.SavedQueries}"
                            ListQueryType="{Binding UpvotePostsModel.ListQueryType}" />
                    </Expander>
                    <StackPanel Orientation="Vertical">

                        <!--  JobConfiguration  Expander  -->
                        <Expander
                            Margin="5,5"
                            Header="{DynamicResource LangKeyJobConfiguration}"
                            IsExpanded="True">
                            <DHCustomControl:JobConfigControl JobConfiguration="{Binding UpvotePostsModel.JobConfiguration}" />
                        </Expander>

                        <Expander
                            Margin="5,5"
                            Header="{DynamicResource LangKeyPostFilters}"
                            IsExpanded="True">
                            <StackPanel Margin="10,5" Orientation="Vertical">
                                <!--  Comment  -->
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Name="CommentCheckBox"
                                        Margin="0,5,5,5"
                                        Content="Comments Count Range From"
                                        IsChecked="{Binding UpvotePostsModel.PostFilterModel.FilterComments}" />
                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.PostFilterModel.FilterComments}"
                                        Minimum="0"
                                        Value="{Binding UpvotePostsModel.PostFilterModel.CommentsCountRange.StartValue}" />
                                    <Label
                                        Margin="0,5,0,0"
                                        VerticalAlignment="Center"
                                        Content="{DynamicResource LangKeyTo}" />

                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.PostFilterModel.FilterComments}"
                                        Minimum="{Binding UpvotePostsModel.PostFilterModel.CommentsCountRange.StartValue}"
                                        Value="{Binding UpvotePostsModel.PostFilterModel.CommentsCountRange.EndValue}" />
                                </StackPanel>

                                <!--  Like  -->
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Name="chkLike"
                                        Margin="0,5,5,5"
                                        Content="Upvotes Count Range From"
                                        IsChecked="{Binding UpvotePostsModel.PostFilterModel.FilterLikes}" />
                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.PostFilterModel.FilterLikes}"
                                        Minimum="0"
                                        Value="{Binding UpvotePostsModel.PostFilterModel.LikesCountRange.StartValue}" />
                                    <Label
                                        Margin="0,5,0,0"
                                        VerticalAlignment="Center"
                                        Content="{DynamicResource LangKeyTo}" />

                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.PostFilterModel.FilterLikes}"
                                        Minimum="{Binding UpvotePostsModel.PostFilterModel.LikesCountRange.StartValue}"
                                        Value="{Binding UpvotePostsModel.PostFilterModel.LikesCountRange.EndValue}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Margin="0,5,5,5"
                                        Content="PostOwner Follower Count Range From"
                                        IsChecked="{Binding UpvotePostsModel.IsCheckPostOwnerFollowerCount}" />
                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.IsCheckPostOwnerFollowerCount}"
                                        Minimum="0"
                                        Value="{Binding UpvotePostsModel.PostOwnerFollowerCount.StartValue}" />
                                    <Label
                                        Margin="0,5,0,0"
                                        VerticalAlignment="Center"
                                        Content="{DynamicResource LangKeyTo}" />

                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.IsCheckPostOwnerFollowerCount}"
                                        Minimum="{Binding UpvotePostsModel.PostOwnerFollowerCount.StartValue}"
                                        Value="{Binding UpvotePostsModel.PostOwnerFollowerCount.EndValue}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Margin="0,5,5,5"
                                        Content="Share Count Range From"
                                        IsChecked="{Binding UpvotePostsModel.IsCheckShareCount}" />
                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.IsCheckShareCount}"
                                        Minimum="0"
                                        Value="{Binding UpvotePostsModel.ShareCountRange.StartValue}" />
                                    <Label
                                        Margin="0,5,0,0"
                                        VerticalAlignment="Center"
                                        Content="{DynamicResource LangKeyTo}" />

                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.IsCheckShareCount}"
                                        Minimum="{Binding UpvotePostsModel.ShareCountRange.StartValue}"
                                        Value="{Binding UpvotePostsModel.ShareCountRange.EndValue}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox
                                        Margin="0,5,5,5"
                                        Content="Views Count Range From"
                                        IsChecked="{Binding UpvotePostsModel.IsCheckViewsCount}" />
                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.IsCheckViewsCount}"
                                        Minimum="0"
                                        Value="{Binding UpvotePostsModel.ViewsCountRange.StartValue}" />
                                    <Label
                                        Margin="0,5,0,0"
                                        VerticalAlignment="Center"
                                        Content="{DynamicResource LangKeyTo}" />

                                    <Controls:NumericUpDown
                                        Margin="0,5,0,0"
                                        IsEnabled="{Binding UpvotePostsModel.IsCheckViewsCount}"
                                        Minimum="{Binding UpvotePostsModel.ViewsCountRange.StartValue}"
                                        Value="{Binding UpvotePostsModel.ViewsCountRange.EndValue}" />
                                </StackPanel>
                            </StackPanel>
                        </Expander>
                        <Expander
                            Margin="5,5"
                            Header="{DynamicResource LangKeyManageBlacklistedUsers}"
                            IsExpanded="True"
                            Visibility="{Binding UpvotePostsModel.IsEnableAdvancedUserMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <QDcustomControl:ManageBlacklistControl
                                    IsChkGroupBlacklist="{Binding UpvotePostsModel.IsChkUpvotePostGroupBlackList}"
                                    IsChkPrivateBlacklist="{Binding UpvotePostsModel.IsChkUpvotePostPrivateBlacklist}"
                                    IsChkSkipGroupBlacklist="{Binding UpvotePostsModel.IsChkUpvotePostSkipGroupBlacklist}"
                                    IsChkSkipPrivateBlacklist="{Binding UpvotePostsModel.IsChkUpvotePostSkipPrivateBlacklist}" />

                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <StackPanel VerticalAlignment="Bottom">
            <Grid Name="FooterGrid" Margin="5,0">
                <DHCustomControl:FooterControl
                    x:Name="UpvotePostsFooter"
                    Height="96"
                    VerticalAlignment="Top"
                    CampaignManager="{Binding CampaignButtonContent}"
                    CreateCampaignCommand="{Binding CreateCampaignCommand}"
                    NoOfAccountsSelected="{Binding SelectedAccountCount, Mode=TwoWay}"
                    SelectAccountCommand="{Binding SelectAccountCommand}" />
            </Grid>
        </StackPanel>


        <!--  Help section  -->
        <!--<Controls:FlyoutsControl>
            <Controls:Flyout Header="{DynamicResource LangKeyModuleDescription}" Position="Right"
                             Visibility="Collapsed" Name="HelpFlyout" Theme="Accent" FontWeight="Bold"
                             Foreground="White" Width="300" BorderBrush="Black" BorderThickness="2"
                             DataContext="{Binding ElementName=UpvotePostsFooter,Path=DataContext}"
                             IsOpen="{Binding IsOpen,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                             Background="{DynamicResource AccentBaseColorBrush}" FontSize="10">
                <DHCustomControl:HelpControl
                    ModuleDescription="{DynamicResource LangKeyModuleDescriptionQuoraAutoUpvoteAnswer}"
                    VideoTutorial="{Binding VideoTutorialLink}" KnowledgeBaseLink="{Binding KnowledgeBaseLink}"
                    ContactSupportLink="{Binding ContactSupportLink}"
                    DataContext="{Binding ElementName=UpvotePostsFooter,Path=DataContext}" />
            </Controls:Flyout>
        </Controls:FlyoutsControl>-->

    </Grid>
</local:UpvotePostsBase>
