<UserControl x:Class="QuoraDominatorUI.CustomControl.AccountReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Loaded="AccountReport_OnLoaded">
    <StackPanel>
        <Grid Background="{DynamicResource AccentBaseColorBrush}" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Background="{DynamicResource AccentBaseColorBrush}"
                        Foreground="White" Margin="0,0,5,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="15" Height="15" Fill="White" Margin="10,0" VerticalAlignment="Center"
                                       ToolTip="{DynamicResource LangKeyRefresh}" MouseDown="Refresh_OnMouseDown">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_refresh}" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Margin="5,0" Text="{DynamicResource LangKeyRefresh}" />
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Background="{DynamicResource AccentBaseColorBrush}"
                        Click="BtnDeleteAll_Click"
                        Foreground="White" Margin="0,0,15,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="12" Height="15" Fill="White">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Visual="{StaticResource appbar_delete}" Stretch="Fill" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Margin="5,0" Text="{DynamicResource LangKeyDeleteAll}" />
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Background="{DynamicResource AccentBaseColorBrush}" Click="BtnExport_Click"
                        Foreground="White" Margin="0,0,15,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="15" Height="15" Fill="White">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Visual="{StaticResource appbar_inbox_out}" Stretch="Fill" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Margin="5,0" Text="{DynamicResource LangKeyExportAll}" />
                        </StackPanel>
                    </Button.Content>
                </Button>
                <!--<Button  Background="{DynamicResource AccentBaseColorBrush}" Name="BtnImport"
                     Click="BtnImport_Click"
                     Foreground="White" Margin="0,0,15,0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="15" Height="15" Fill="White">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Visual="{StaticResource appbar_inbox_in}" Stretch="Fill"/>
                                </Rectangle.OpacityMask>
                            </Rectangle>
                            <TextBlock Margin="5,0" Text="{DynamicResource LangKeyImport}"/>
                        </StackPanel>
                    </Button.Content>
                </Button>-->
            </StackPanel>

            <ComboBox VerticalAlignment="Center" Width="200" x:Name="CmbAccounts" Margin="10,0"
                      SelectionChanged="CmbAccounts_SelectionChanged" Background="Transparent"
                      Foreground="White" BorderThickness="1" SelectedIndex="0"
                      BorderBrush="AntiqueWhite" Grid.Column="1" HorizontalAlignment="Right" />

        </Grid>
        <DataGrid Margin="5" x:Name="ReportGrid" AutoGeneratingColumn="ReportGrid_AutoGeneratingColumn"
                  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                  CanUserAddRows="False" MaxHeight="450">
            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>

                </Style>
            </DataGrid.CellStyle>

        </DataGrid>
    </StackPanel>
</UserControl>