<UserControl x:Class="PinDominator.PDViews.Tools.Repost.RepostReports"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PinDominator.PDViews.Tools.Repost"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Column="0">
            <Button Content="{DynamicResource LangKeyDeleteAll}" Background="#003569" Foreground="White" Margin="0,0,15,0"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <Button Content="{DynamicResource LangKeyExportAll}" Background="#003569" Foreground="White" Margin="0,0,15,0"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <DataGrid>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource LangKeyDate}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource LangKeyNumOfLikes}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource LangKeyKeyword}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource LangKeyNumOfComments}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource LangKeyPostCaption}" Width="*"/>
                    <DataGridTemplateColumn Header="{DynamicResource LangKeyLink}" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="{DynamicResource LangKeyDelete}" Background="#003569" Foreground="White" Margin="0,0,15,0"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    <Button Content="{DynamicResource LangKeyBrowse}" Background="#003569" Foreground="White" Margin="0,0,15,0"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    <Button Content="{DynamicResource LangKeyBrowseEmbedded}" Background="#003569" Foreground="White" Margin="0,0,15,0"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    <Button Content="{DynamicResource langCopy}" Background="#003569" Foreground="White" Margin="0,0,15,0"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
