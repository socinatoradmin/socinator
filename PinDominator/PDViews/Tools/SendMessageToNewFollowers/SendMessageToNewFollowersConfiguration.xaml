<local:SendMessageToNewFollowersConfigurationBase
    x:Class="PinDominator.PDViews.Tools.SendMessageToNewFollowers.SendMessageToNewFollowersConfiguration"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PinDominator.PDViews.Tools.SendMessageToNewFollowers"
    mc:Ignorable="d"
    x:Name="SendMessageToNewFollowersConfigurationUserControl"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:customControl="clr-namespace:DominatorUIUtility.CustomControl;assembly=DominatorUIUtility"
    xmlns:customControl1="clr-namespace:PinDominator.CustomControl"
    xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction
                Command="{Binding LoadedCommand,ElementName=SendMessageToNewFollowersConfigurationUserControl}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <customControl:AccountGrowthModeHeader Name="AccountGrowthHeader"
                                               VerticalAlignment="Top" SelectedIndex="0"
                                               SelectionChangedCommand="{Binding SelectionChangedCommand,ElementName=SendMessageToNewFollowersConfigurationUserControl}"
                                               SaveCommand="{Binding SaveConfigurationsCommand}"
                                               VideoTutorialLink="{Binding VideoTutorialLink}" />

        <ScrollViewer Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource LangKeyStatus}" FontWeight="Bold" HorizontalAlignment="Left"
                                   Margin="5" />
                        <controls:ToggleSwitch OnLabel="Active﻿" OffLabel="Stopped" x:Name="TglStatus"
                                               FontSize="12" Margin="5"
                                               Style="{StaticResource MahApps.Metro.Styles.ToggleSwitch.Win10}"
                                               IsChecked="{Binding SendMessageToNewFollowersModel.IsAccountGrowthActive}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction
                                        Command="{Binding StatusChangedCommand,ElementName=SendMessageToNewFollowersConfigurationUserControl}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </controls:ToggleSwitch>
                    </StackPanel>

                </StackPanel>

                <Grid Grid.Row="1">
                    <StackPanel Orientation="Vertical">

                        <!-- Manage Messege -->

                        <Expander Header="{DynamicResource LangKeyManageMessages}" Margin="5,5">
                            <StackPanel>
                                <CheckBox Content="{DynamicResource LangKeyMakeMessageAsSpinText}" Margin="0,5,5,10"
                                          IsChecked="{Binding SendMessageToNewFollowersModel.IsMakeMessageAsSpinText}" />

                                <customControl:InputBoxControl x:Name="SendMessageToNewFollowersConfigurationInputBox"
                                                               SaveCommand="{Binding UploadNotesCommand}"
                                                               CommandParameter="{Binding ElementName=SendMessageToNewFollowersConfigurationInputBox}"
                                                               InputText="{Binding SendMessageToNewFollowersModel.Message}" />
                            </StackPanel>
                        </Expander>

                        <!--Other Configurations filter Expender-->
                        <!--<Expander Header="{DynamicResource LangKeyOtherConfigurations}" Margin="5,5">
                            <CheckBox Margin="20,0,0,0" Content="{DynamicResource LangKeySendPinAsAMessage}"
                                      IsChecked="{Binding SendMessageToNewFollowersModel.IsSendPinAsAMessage}" />
                        </Expander>-->

                        <!-- After JobConfiguration  Expender-->

                        <Expander Header="{DynamicResource LangKeyJobConfiguration}" Margin="5,5">
                            <customControl:JobConfigControl
                                JobConfiguration="{Binding SendMessageToNewFollowersModel.JobConfiguration}" />
                        </Expander>

                        <!-- Other Configurations filter Expander-->
                        <!--
                        <Expander Header="{DynamicResource LangKeyOtherConfigurations}" Margin="5,5">
                            <customControl:OtherConfig OtherConfigFilter="{Binding OtherConfigModel}"/>
                        </Expander>-->

                        <!-- After User Filters Expender-->

                        <Expander Header="{DynamicResource LangKeyUserFilters}" Margin="5,5"
                                  Visibility="{Binding SendMessageToNewFollowersModel.IsEnableAdvancedUserMode,Converter={StaticResource BooleanToVisibilityConverter}}">
                            <customControl1:UserFiltersControl x:Name="UserFiltersControl"
                                                               UserFilter="{Binding SendMessageToNewFollowersModel.UserFilterModel}" />
                        </Expander>


                        <!--Manage Blacklist Whitelist Users  expander-->

                        <Expander Header="{DynamicResource LangKeyManageBlacklistWhitelistUsers}" Margin="5,5"
                                  Visibility="{Binding SendMessageToNewFollowersModel.IsEnableAdvancedUserMode,Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <CheckBox Content="{DynamicResource LangKeySkipBlacklistedUsers}" Margin="10,5"
                                          IsChecked="{Binding SendMessageToNewFollowersModel.IsChkSkipBlackListedUser}" />
                                <StackPanel Margin="25,5"
                                            Visibility="{Binding SendMessageToNewFollowersModel.IsChkSkipBlackListedUser, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Orientation="Horizontal">
                                    <CheckBox Content="{DynamicResource LangKeyPrivateBlacklist}" Margin="5,0"
                                              IsChecked="{Binding SendMessageToNewFollowersModel.IsChkPrivateBlackList}" />
                                    <CheckBox Content="{DynamicResource LangKeyGroupBlacklist}" Margin="5,0"
                                              IsChecked="{Binding SendMessageToNewFollowersModel.IsChkGroupBlackList}" />
                                </StackPanel>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Grid>

            </Grid>
        </ScrollViewer>
    </Grid>
</local:SendMessageToNewFollowersConfigurationBase>