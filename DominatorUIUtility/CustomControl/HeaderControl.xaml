<UserControl
    x:Class="DominatorUIUtility.CustomControl.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinHeight="40"
    mc:Ignorable="d">
    <Border
        Margin="5,0,19,0"
        Background="{DynamicResource AccentBaseColorBrush}"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        CornerRadius="8,8,0,0">
        <Grid Name="mainGrid">
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    Margin="10,0"
                    VerticalAlignment="Center"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Text="{DynamicResource LangKeyNameYourCampaign}" />
                <TextBox
                    Name="txtblkCampaignName"
                    Width="300"
                    Margin="100,0,0,0"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="White"
                    CaretBrush="White"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="White"
                    IsEnabled="{Binding IsCampaignNameEditable}"
                    Text="{Binding CampaignName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <ToggleButton
                    Name="ClpsExpnd"
                    VerticalAlignment="Center"
                    Click="ClpsExpnd_OnClick"
                    Cursor="Hand"
                    Foreground="White">
                    <ToggleButton.Style>
                        <Style BasedOn="{StaticResource MetroFlatToggleButton}" TargetType="{x:Type ToggleButton}">
                            <Setter Property="IsChecked" Value="{Binding IsExpanded, UpdateSourceTrigger=PropertyChanged}" />
                            <Style.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Content" Value="{DynamicResource LangKeyCollapseAll}" />
                                    <Setter Property="Background" Value="{DynamicResource AccentBaseColorBrush}" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter Property="Content" Value="{DynamicResource LangKeyExpandAll}" />
                                    <Setter Property="Background" Value="{DynamicResource AccentBaseColorBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
                <Button
                    Name="btnCancelEdit"
                    Width="130"
                    Height="28"
                    Margin="7,0,10,0"
                    Background="Transparent"
                    Command="{Binding CancelEditCommand}"
                    Cursor="Hand"
                    Style="{StaticResource ButtonStyle}"
                    Visibility="{Binding IsCancelEditVisible}">
                    <Button.Template>
                        <ControlTemplate>
                            <Border
                                Background="#ef1d1d"
                                BorderBrush="White"
                                BorderThickness="1"
                                CornerRadius="5">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <Rectangle
                                        Width="15"
                                        Height="15"
                                        Fill="White">
                                        <Rectangle.OpacityMask>
                                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_cancel}" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                    <TextBlock Margin="6,0" Text="{DynamicResource LangKeyCancelEditing}" />
                                </StackPanel>
                            </Border>
                        </ControlTemplate>

                    </Button.Template>
                </Button>

                <Button
                    Background="Transparent"
                    Command="{Binding InfoCommand}"
                    Cursor="Hand">
                    <Button.Content>
                        <Rectangle
                            x:Name="Info"
                            Width="25"
                            Height="25"
                            Margin="10,0"
                            Fill="White"
                            ToolTip="{DynamicResource LangKeyInfo}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_information_circle}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button.Content>
                </Button>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>