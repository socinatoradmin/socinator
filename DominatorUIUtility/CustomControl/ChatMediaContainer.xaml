<UserControl x:Class="DominatorUIUtility.CustomControl.ChatMediaContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DominatorUIUtility.CustomControl"
             xmlns:converter="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:ChatImageCountToColumnConverter x:Key="ChatImageCountToColumnConverter" />
    </UserControl.Resources>
    <Grid Name="MediaContainer">
        <Border CornerRadius="4">
            <ListView BorderBrush="Transparent" x:Name="AccountListView" ScrollViewer.IsDeferredScrollingEnabled="True"
                      VirtualizingPanel.IsContainerVirtualizable="True" Grid.Row="1"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      AlternationCount="{Binding Items.Count, RelativeSource={RelativeSource Self}}"
                      ItemsSource="{Binding ListMediaUrls}">
                <ListView.Resources>
                    <SolidColorBrush x:Key="ItemBackgroundHover" Color="Transparent" />
                </ListView.Resources>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid
                            Columns="{Binding ListMediaUrls,Converter={StaticResource ChatImageCountToColumnConverter}}" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ItemBackgroundHover}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <!--<Border Height="60" Width="60" BorderBrush="Green" BorderThickness="4" CornerRadius="16">-->
                        <Rectangle RadiusX="10" RadiusY="10" Width="200"
                                   Height="200" Margin="5" Cursor="Hand">
                            <Rectangle.Fill>
                                <!--<Grid Background="Red">-->
                                <ImageBrush x:Name="myImage" ImageSource="{Binding}" />
                                <!--</Grid>-->
                            </Rectangle.Fill>
                        </Rectangle>
                        <!--</Border>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>