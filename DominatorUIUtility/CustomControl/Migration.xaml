<UserControl x:Class="DominatorUIUtility.CustomControl.Migration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <StackPanel>
        <CheckBox Content="{DynamicResource LangKeyAccounts}" Margin="5" Click="AccountClick" Name="account" />
        <CheckBox Content="{DynamicResource LangKeyCampaign}" Margin="5" />
        <CheckBox Content="{DynamicResource LangKeyCampaignData}" Margin="5" />
        <CheckBox Content="{DynamicResource LangKeyBlacklist}" Margin="5" />
        <CheckBox Content="{DynamicResource LangKeyWhitelist}" Margin="5" />
        <CheckBox Content="{DynamicResource LangKeyFollowers}" Margin="5" />
    </StackPanel>

</UserControl>