<UserControl
    x:Class="DominatorUIUtility.CustomControl.ProxyManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:converter="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:enums="clr-namespace:DominatorHouseCore.Enums;assembly=DominatorHouseCore"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:models="clr-namespace:DominatorHouseCore.Models;assembly=DominatorHouseCore"
    xmlns:uiExtensions="clr-namespace:DominatorHouseCore.UIExtensions;assembly=DominatorHouseCore"
    xmlns:validation="clr-namespace:DominatorHouseCore.Utility;assembly=DominatorHouseCore"
    x:Name="MainGrid">
    <UserControl.Resources>
        <converter:WorkingStringToVisibilityConverter x:Key="WorkingStringToVisibilityConverter" />
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverterInversed" IsInversed="True" />
        <converter:ProxyManagerSourceConverter x:Key="ProxyManagerSourceConverter" />
        <converter:SocialNetworkToVisualBrushConverter x:Key="SocialNetworkToVisualBrushConverter" />
        <converter:SocialNetworkToColorBrushConverter x:Key="SocialNetworkToColorBrushConverter" />
        <ControlTemplate x:Key="ErrorTemplate">
            <StackPanel Orientation="Vertical">
                <AdornedElementPlaceholder x:Name="ErrorHolder">
                    <Border BorderBrush="red" BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=error}" Value="Invalid IP address">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </AdornedElementPlaceholder>
                <TextBlock
                    Name="error"
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Foreground="Red"
                    Text="{Binding ElementName=ErrorHolder, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=error}" Value="Invalid IP address">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>

                </TextBlock>
            </StackPanel>
        </ControlTemplate>

        <CollectionViewSource x:Key="ProxyView">
            <!--  uiExtensions:CollectionViewSourceExtensions.GroupBy="{Binding Groups, IsAsync=True}">  -->
            <CollectionViewSource.Source>
                <MultiBinding Converter="{StaticResource ProxyManagerSourceConverter}">
                    <Binding IsAsync="True" Path="LstProxyManagerModel" />
                    <Binding
                        IsAsync="True"
                        Path="IsChecked"
                        Source="{x:Reference ShowProxiesWithError}" />
                    <Binding
                        IsAsync="True"
                        Path="IsChecked"
                        Source="{x:Reference ChkShowUnassignedProxies}" />
                    <Binding
                        IsAsync="True"
                        Path="IsChecked"
                        Source="{x:Reference FilterByGroup}" />
                    <Binding
                        IsAsync="True"
                        Path="SelectedItem"
                        Source="{x:Reference GroupCmb}" />
                    <Binding
                        IsAsync="True"
                        Path="Text"
                        Source="{x:Reference FilterTb}" />
                    <Binding IsAsync="True" Path="IsAllProxySelected" />
                    <Binding IsAsync="True" Path="LstProxyManagerModel.Count" />
                </MultiBinding>
            </CollectionViewSource.Source>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0">
            <Border Background="{DynamicResource AccentBaseColorBrush}" CornerRadius="8,8,0,0">
                <StackPanel Orientation="Horizontal">

                    <StackPanel HorizontalAlignment="Stretch" Orientation="Horizontal">
                        <Button
                            x:Name="btnSaveSetting"
                            Height="30"
                            Margin="10,5"
                            Background="{DynamicResource AccentBaseColorBrush}"
                            Command="{Binding SaveSettingCommand}"
                            Foreground="White"
                            Visibility="{Binding RefreshEnable}">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <Rectangle
                                        Width="16"
                                        Height="15"
                                        VerticalAlignment="Center"
                                        Fill="White">
                                        <Rectangle.OpacityMask>
                                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_save}" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                    <TextBlock Margin="5,0" Text="{DynamicResource LangKeySaveSettings}" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>

                    <Button
                        x:Name="btnImportProxies"
                        Height="30"
                        Margin="10,5"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Command="{Binding ImportProxyCommand}"
                        Foreground="White"
                        ToolTip="{DynamicResource LangKeyImportProxyDescription}">
                        <Button.Content>
                            <VirtualizingStackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="16"
                                    Height="15"
                                    VerticalAlignment="Center"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_inbox_in}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="5,0" Text="{DynamicResource LangKeyImportProxies}" />
                            </VirtualizingStackPanel>
                        </Button.Content>
                    </Button>

                    <Button
                        x:Name="btnExportProxies"
                        Height="30"
                        Margin="10,5"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Command="{Binding ExportProxyCommand}"
                        Foreground="White">
                        <Button.Content>
                            <VirtualizingStackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="16"
                                    Height="15"
                                    VerticalAlignment="Center"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_inbox_out}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="5,0" Text="{DynamicResource LangKeyExportProxies}" />
                            </VirtualizingStackPanel>
                        </Button.Content>
                    </Button>

                    <Button
                        x:Name="btnAddProxy"
                        Height="30"
                        Margin="10,5"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Command="{Binding AddProxyCommand}"
                        Foreground="White"
                        IsEnabled="{Binding IsAddProxyEnabled}">
                        <Button.Content>
                            <VirtualizingStackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="16"
                                    Height="15"
                                    VerticalAlignment="Center"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_add}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="5,0" Text="{DynamicResource LangKeyAddProxy}" />
                            </VirtualizingStackPanel>
                        </Button.Content>
                    </Button>

                </StackPanel>
            </Border>

            <StackPanel Margin="5" Orientation="Horizontal">

                <CheckBox
                    x:Name="ChkHideUnhideSocialProfiles"
                    Margin="5"
                    Content="{DynamicResource LangKeyHideAssignedSocialProfiles}"
                    IsChecked="{Binding SettingsModel.HideAssignedSocialMediaProfiles}" />
                <CheckBox
                    x:Name="ChkHideUnhideUserPassword"
                    Margin="5"
                    Content="{DynamicResource LangKeyHideUsernameAndPassword}"
                    IsChecked="{Binding SettingsModel.HideUserNameAndPassword}" />
                <CheckBox
                    x:Name="ShowByGroup"
                    Margin="5"
                    Command="{Binding ShowByGroupCommand}"
                    CommandParameter="{Binding ElementName=ProxyDataGrid}"
                    Content="{DynamicResource LangKeyShowByGroup}"
                    IsChecked="{Binding IsShowByGroup}" />
                <CheckBox
                    x:Name="FilterByGroup"
                    Margin="5"
                    Content="{DynamicResource LangKeyFilterByGroup}"
                    IsChecked="{Binding SettingsModel.FilterByGroup}" />
                <CheckBox
                    x:Name="ShowProxiesWithError"
                    Margin="5"
                    Content="{DynamicResource LangKeyShowOnlyProxiesWithError}"
                    IsChecked="{Binding SettingsModel.ShowOnlyProxyWithError}" />
                <CheckBox
                    x:Name="ChkShowUnassignedProxies"
                    Margin="5"
                    Content="{DynamicResource LangKeyShowOnlyUnassignedProxies}"
                    IsChecked="{Binding SettingsModel.ShowOnlyUnAssignedProxy}" />

            </StackPanel>

            <StackPanel Margin="5" Orientation="Horizontal">
                <TextBlock
                    Margin="5"
                    VerticalAlignment="Center"
                    Text="{DynamicResource LangKeyFilter}" />
                <TextBox
                    x:Name="FilterTb"
                    Width="200"
                    Height="25"
                    ToolTip="Filter by proxy name or IP" />
                <TextBlock
                    Margin="20,5"
                    VerticalAlignment="Center"
                    Text="{DynamicResource LangKeyURLToUseToVerifyTheProxies}" />
                <TextBox Width="200" Height="25">
                    <TextBox.Text>
                        <Binding Path="SettingsModel.VerificationUrl" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validation:UrlValidator ValidatesOnTargetUpdated="True" />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <CheckBox
                    Margin="5"
                    Content="{DynamicResource LangKeyAssignRandomProxyToUnassignedAccounts}"
                    IsChecked="{Binding IsRandomSelected}">
                    <CheckBox.Style>
                        <Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="CheckBox">
                            <Style.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Command" Value="{Binding AssignRandomProxyCommand}" />
                                    <Setter Property="Background" Value="{DynamicResource ListItemsMouseHoverColorAccordingTheme}" />
                                    <Setter Property="Foreground" Value="{DynamicResource GreenColorAccordingTheme}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>

            </StackPanel>
            <StackPanel Margin="5,5,5,10" Orientation="Horizontal">

                <StackPanel Orientation="Horizontal" Visibility="{Binding ElementName=FilterByGroup, Path=IsChecked, Converter={StaticResource BooleanToVisibilityConverter}, Mode=TwoWay}">
                    <TextBlock
                        Margin="5,5"
                        VerticalAlignment="Center"
                        Text="{DynamicResource LangKeySelectGroup}" />
                    <ComboBox
                        x:Name="GroupCmb"
                        Width="150"
                        Controls:TextBoxHelper.ClearTextButton="True"
                        ItemsSource="{Binding Groups}" />

                </StackPanel>
                <CheckBox
                    Margin="5"
                    Content="{DynamicResource LangKeyAdd}"
                    IsChecked="{Binding SettingsModel.IsNumOfAccountPerProxy}" />
                <Controls:NumericUpDown Minimum="1" Value="{Binding SettingsModel.NumOfAccountPerProxy}" />
                <Label Content="{DynamicResource LangKeyAccountPerProxy}" />

                <CheckBox
                    Margin="10,5"
                    Content="{DynamicResource LangKeyDontLoginAccountAfterProxyManagement}"
                    IsChecked="{Binding SettingsModel.DontLogin}" />

            </StackPanel>
            <Border Background="{DynamicResource AccentBaseColorBrush}" CornerRadius="8,8,0,0">
                <StackPanel Orientation="Horizontal">
                    <Button
                        x:Name="btnDeleteSelected"
                        Height="30"
                        Margin="10,5"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Command="{Binding DeleteCommand}"
                        Foreground="White">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="15"
                                    Height="18"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_delete}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="5,0" Text="{DynamicResource LangKeySelectedOnly}" />
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button
                        x:Name="btnExportSelected"
                        Height="30"
                        Margin="10,5"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Command="{Binding ExportProxyCommand}"
                        Foreground="White">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="16"
                                    Height="15"
                                    VerticalAlignment="Center"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_inbox_out}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="5,0" Text="{DynamicResource LangKeyExportSelected}" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <StackPanel Orientation="Horizontal">
                        <Button
                            x:Name="btnVarifySelected"
                            Height="30"
                            Margin="10,5"
                            Background="{DynamicResource AccentBaseColorBrush}"
                            Command="{Binding VerifyProxyCommand}"
                            Foreground="White"
                            IsEnabled="{Binding VerifyProxiesViewModel.IsIdle, IsAsync=True}">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <Rectangle
                                        Width="16"
                                        Height="15"
                                        VerticalAlignment="Center"
                                        Fill="White">
                                        <Rectangle.OpacityMask>
                                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_social_pocket}" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                    <TextBlock
                                        Margin="5,0"
                                        Text="{DynamicResource LangKeyVerifySelected}"
                                        Visibility="{Binding VerifyProxiesViewModel.IsIdle, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Margin="5,0" Visibility="{Binding VerifyProxiesViewModel.IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}Verified {0} from {1}">
                                                <Binding IsAsync="True" Path="VerifyProxiesViewModel.Verified" />
                                                <Binding IsAsync="True" Path="VerifyProxiesViewModel.Total" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button
                            x:Name="btnRefresh"
                            Height="30"
                            Margin="10,5"
                            Background="{DynamicResource AccentBaseColorBrush}"
                            Command="{Binding RefreshProxyCommand}"
                            Foreground="White"
                            Visibility="{Binding RefreshEnable}">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <Rectangle
                                        Width="16"
                                        Height="15"
                                        VerticalAlignment="Center"
                                        Fill="White">
                                        <Rectangle.OpacityMask>
                                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_refresh}" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                    <TextBlock Margin="5,0" Text="{DynamicResource LangKeyRefresh}" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>


        <DataGrid
            x:Name="ProxyDataGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            Background="Transparent"
            CanUserAddRows="False"
            EnableRowVirtualization="True"
            Foreground="White"
            GridLinesVisibility="All"
            ItemsSource="{Binding Source={StaticResource ProxyView}}"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ScrollViewer.IsDeferredScrollingEnabled="True"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            SelectedItem="{Binding ProxyManagerModel}"
            VirtualizingPanel.IsVirtualizingWhenGrouping="True"
            VirtualizingStackPanel.IsVirtualizing="True"
            VirtualizingStackPanel.VirtualizationMode="Recycling">
            <DataGrid.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel ScrollUnit="Pixel" VirtualizationMode="Recycling" />
                </ItemsPanelTemplate>
            </DataGrid.ItemsPanel>
            <DataGrid.Resources>

                <DataTemplate x:Key="AccountsAssignedToTemplate">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <VirtualizingStackPanel Orientation="Horizontal">
                            <ContentControl Margin="10,0" VerticalAlignment="Center">
                                <ContentControl.Style>
                                    <Style TargetType="{x:Type ContentControl}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Facebook}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="#003569">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_social_facebook_variant}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Instagram}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="Red">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource Instagram}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Twitter}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="#3897F0">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_twitter_bird}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Pinterest}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="Red">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_social_pinterest}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Quora}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="Red">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource Quora}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Reddit}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="OrangeRed">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_social_reddit}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.LinkedIn}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="DodgerBlue">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_social_linkedin_variant}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Tumblr}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="16"
                                                            Height="20"
                                                            Fill="DimGray">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_social_tumblr}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                            <!--<DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.Gplus}">
                                                <Setter Property="Content" >
                                                    <Setter.Value>
                                                        <Rectangle Fill="#FF0000" Width="20" Height="20">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Visual="{StaticResource appbar_googleplus}" Stretch="Fill" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>-->

                                            <DataTrigger Binding="{Binding Path=AccountNetwork}" Value="{x:Static enums:SocialNetworks.YouTube}">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <Rectangle
                                                            Width="20"
                                                            Height="20"
                                                            Fill="#FF0000">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_youtube}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>

                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                            <TextBlock
                                x:Name="txtblkaccount"
                                VerticalAlignment="Center"
                                Text="{Binding UserName}" />
                        </VirtualizingStackPanel>
                        <Button
                            x:Name="btnRemoveAccountFromProxy"
                            Grid.Column="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            Command="{Binding Source={x:Reference MainGrid}, Path=DataContext.RemoveAccountFromProxyCommand}"
                            CommandParameter="{Binding}"
                            Content="{DynamicResource LangKeyRemove}"
                            FontWeight="SemiBold"
                            Foreground="{Binding ElementName=txtblkaccount, Path=Foreground}" />
                    </Grid>
                </DataTemplate>

                <DataTemplate x:Key="AccountsToBeAssignTemplate">
                    <VirtualizingStackPanel Orientation="Horizontal">
                        <CheckBox
                            x:Name="Account"
                            Command="{Binding DataContext.AccountToAddToProxyCommand, Source={x:Reference MainGrid}}"
                            CommandParameter="{Binding}" />
                        <Rectangle
                            Width="20"
                            Height="20"
                            Margin="10,0"
                            VerticalAlignment="Center"
                            Fill="{Binding AccountNetwork, Converter={StaticResource SocialNetworkToColorBrushConverter}}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{Binding AccountNetwork, Converter={StaticResource SocialNetworkToVisualBrushConverter}}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock
                            x:Name="txtblkaccount"
                            VerticalAlignment="Center"
                            Text="{Binding UserName}" />
                    </VirtualizingStackPanel>
                </DataTemplate>
                <Style TargetType="{x:Type DataGridCell}">
                    <Setter Property="Foreground" Value="{DynamicResource MetroDataGrid.InactiveSelectionHighlightBrush}" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextColorBrushAccordingTheme}" />
                            <Setter Property="Background" Value="{DynamicResource ListItemsMouseHoverColorAccordingTheme}" />
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                        </Trigger>
                    </Style.Triggers>
                </Style>

            </DataGrid.Resources>
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Margin" Value="0,0,0,5" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Expander Margin="5">
                                            <Expander.Header>
                                                <VirtualizingStackPanel Orientation="Horizontal">
                                                    <TextBlock
                                                        VerticalAlignment="Center"
                                                        FontWeight="Bold"
                                                        Foreground="{DynamicResource TextColorBrushAccordingTheme}"
                                                        Text="{Binding Name}" />
                                                    <TextBlock
                                                        Margin="5,0,0,0"
                                                        VerticalAlignment="Center"
                                                        FontWeight="Bold"
                                                        Foreground="{DynamicResource TextColorBrushAccordingTheme}"
                                                        Text="{Binding ItemCount, StringFormat=({0})}" />
                                                </VirtualizingStackPanel>
                                            </Expander.Header>
                                            <Expander.Content>
                                                <ItemsPresenter />
                                            </Expander.Content>
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>

            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.Header>
                        <CheckBox
                            Name="AllProxyChecked"
                            Content=" #"
                            IsChecked="{Binding DataContext.IsAllProxySelected, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                    </DataGridTemplateColumn.Header>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox
                                Margin="5,0,0,0"
                                Command="{Binding DataContext.SelectProxyCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                Content="{Binding Index}"
                                IsChecked="{Binding IsProxySelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="{DynamicResource LangKeyName}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Text="{Binding AccountProxy.ProxyName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding AccountProxy.ProxyName, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn MinWidth="160" Header="{DynamicResource LangKeyProxyIpPort}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Validation.ErrorTemplate="{StaticResource ErrorTemplate}">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0}:{1}">
                                        <MultiBinding.ValidationRules>
                                            <validation:IPAddressValidation ValidationStep="CommittedValue" />
                                        </MultiBinding.ValidationRules>
                                        <Binding Path="AccountProxy.ProxyIp" UpdateSourceTrigger="PropertyChanged" />
                                        <Binding Path="AccountProxy.ProxyPort" UpdateSourceTrigger="PropertyChanged" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <Canvas>
                                <TextBox
                                    Width="100"
                                    SelectionLength="0"
                                    SelectionStart="0"
                                    Text="{Binding AccountProxy.ProxyIp, UpdateSourceTrigger=PropertyChanged}"
                                    Validation.ErrorTemplate="{StaticResource ErrorTemplate}" />
                                <TextBox
                                    Width="45"
                                    Margin="102,0,0,0"
                                    SelectionLength="0"
                                    SelectionStart="0"
                                    Text="{Binding AccountProxy.ProxyPort, UpdateSourceTrigger=PropertyChanged}"
                                    Validation.ErrorTemplate="{StaticResource ErrorTemplate}" />
                            </Canvas>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="{DynamicResource LangKeyProxyUsername}" Visibility="{Binding IsChecked, Source={x:Reference ChkHideUnhideUserPassword}, Converter={StaticResource BooleanToVisibilityConverterInversed}}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Text="{Binding AccountProxy.ProxyUsername, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding AccountProxy.ProxyUsername, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="{DynamicResource LangKeyProxyPassword}" Visibility="{Binding IsChecked, Source={x:Reference ChkHideUnhideUserPassword}, Converter={StaticResource BooleanToVisibilityConverterInversed}}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Text="{Binding AccountProxy.ProxyPassword, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding AccountProxy.ProxyPassword, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="{DynamicResource LangKeySocialProfiles}" Visibility="{Binding IsChecked, Source={x:Reference ChkHideUnhideSocialProfiles}, Converter={StaticResource BooleanToVisibilityConverterInversed}}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <VirtualizingStackPanel Orientation="Horizontal">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Text="{Binding AccountsAssignedto.Count, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" />
                            </VirtualizingStackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <VirtualizingStackPanel Orientation="Horizontal">
                                <Controls:DropDownButton
                                    x:Name="dropdown"
                                    Width="40"
                                    Height="20"
                                    Command="{Binding DataContext.DropDownCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                    CommandParameter="{Binding}"
                                    Content="{Binding Path=AccountsAssignedto.Count}"
                                    ItemTemplate="{StaticResource AccountsAssignedToTemplate}"
                                    ItemsSource="{Binding AccountsAssignedto}">
                                    <Controls:DropDownButton.Style>
                                        <Style TargetType="{x:Type Controls:DropDownButton}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=AccountsAssignedto.Count}" Value="0">
                                                    <Setter Property="IsEnabled" Value="False" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Controls:DropDownButton.Style>

                                </Controls:DropDownButton>

                                <Controls:DropDownButton
                                    x:Name="AddDropdown"
                                    Width="40"
                                    Height="20"
                                    Margin="10,0"
                                    Command="{Binding DataContext.DropDownCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                    CommandParameter="{Binding}"
                                    Content="{DynamicResource LangKeyAdd}"
                                    IsEnabled="True"
                                    ItemTemplate="{StaticResource AccountsToBeAssignTemplate}"
                                    ItemsSource="{Binding AccountsToBeAssign}"
                                    Visibility="{Binding Status, Converter={StaticResource WorkingStringToVisibilityConverter}, Mode=TwoWay}" />

                            </VirtualizingStackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn
                    Binding="{Binding Status, Mode=OneWay}"
                    Header="{DynamicResource LangKeyStatus}"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Binding="{Binding ResponseTime, Mode=OneWay}"
                    Header="{DynamicResource LangKeyResponseTime}"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Binding="{Binding Failures, Mode=OneWay}"
                    Header="{DynamicResource LangKeyFailures}"
                    IsReadOnly="True" />
                <DataGridTemplateColumn Header="{DynamicResource LangKeyAction}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="{x:Type models:ProxyManagerModel}">
                            <VirtualizingStackPanel Orientation="Horizontal">
                                <Button
                                    Background="Transparent"
                                    Command="{Binding DataContext.UpdateProxyCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                    CommandParameter="{Binding}"
                                    Content="{DynamicResource LangKeyUpdateProxy}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AccentBaseColorBrush}" />
                                <Button
                                    Background="Transparent"
                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                    CommandParameter="{Binding}"
                                    Content="{DynamicResource LangKeyDelete}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AccentBaseColorBrush}" />
                                <Button
                                    Background="Transparent"
                                    Command="{Binding DataContext.VerifyProxyCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                    CommandParameter="{Binding}"
                                    Content="{DynamicResource LangKeyVerifyProxy}"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource AccentBaseColorBrush}" />

                            </VirtualizingStackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!--<DataGridTextColumn Binding="{Binding AccountProxy.ProxyGroup}" Header="{DynamicResource LangKeyGroup}" />-->
                <DataGridTemplateColumn Header="{DynamicResource LangKeyGroup}" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="{DynamicResource TextColorBrushAccordingTheme}" Text="{Binding AccountProxy.ProxyGroup}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>

                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Foreground="{DynamicResource TextColorBrushAccordingTheme}" Text="{Binding AccountProxy.ProxyGroup, UpdateSourceTrigger=PropertyChanged}">
                                <i:Interaction.Triggers>
                                    <!--  🔹 Trigger when focus is lost  -->
                                    <i:EventTrigger EventName="LostFocus">
                                        <converter:InvokeCommandWithArgsAction Command="{Binding DataContext.GroupTextChangedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding AccountProxy}" />
                                        <!--  passes row item  -->
                                    </i:EventTrigger>

                                    <!--  🔹 Trigger when Enter key is pressed  -->
                                    <i:EventTrigger EventName="KeyDown">
                                        <converter:InvokeCommandWithArgsAction Command="{Binding DataContext.GroupTextChangedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>

</UserControl>