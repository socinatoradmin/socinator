<UserControl
    x:Class="DominatorUIUtility.CustomControl.CommentMediaControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enums="clr-namespace:DominatorHouseCore.Enums;assembly=DominatorHouseCore"
    xmlns:local="clr-namespace:DominatorUIUtility.CustomControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <StackPanel Name="MainGrid" x:FieldModifier="public">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock
                    Margin="0,10"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="Bold"
                    Text="{DynamicResource LangKeyCommentText}" />
                <local:InputBoxControl
                    x:Name="InputComment"
                    InputText="{Binding Comments.CommentText}"
                    SaveVisiblity="Collapsed" />
                <StackPanel Margin="10,0,0,0" Orientation="Horizontal">
                    <Button
                        x:Name="btnPhotos"
                        Width="100"
                        Height="30"
                        Margin="5"
                        HorizontalAlignment="Left"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Click="btnPhotos_Click"
                        Foreground="White">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="15"
                                    Height="15"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_image_export}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="6,0" Text="{DynamicResource LangKeyPhoto}" />

                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <Button
                        x:Name="btnVideos"
                        Width="100"
                        Height="30"
                        Margin="5"
                        HorizontalAlignment="Left"
                        Background="{DynamicResource AccentBaseColorBrush}"
                        Click="btnVideos_Click"
                        Foreground="White">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Rectangle
                                    Width="15"
                                    Height="15"
                                    Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_video}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Margin="6,0" Text="{DynamicResource LangKeyVideo}" />

                            </StackPanel>
                        </Button.Content>
                        <Button.Style>
                            <Style BasedOn="{StaticResource ButtonStyle}" TargetType="Button">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Network}" Value="{x:Static enums:SocialNetworks.Instagram}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                    </Button>

                    <StackPanel
                        x:Name="PART_MainStackPanel1"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Visibility="{Binding IsEnablePreviousPointer, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button
                            x:Name="PART_NavigatePreviousImage"
                            Margin="1,0,5,0"
                            VerticalAlignment="Center"
                            Background="{DynamicResource AccentBaseColorBrush}"
                            Click="PART_NavigatePreviousImage_Click"
                            Cursor="Hand"
                            Foreground="White"
                            IsEnabled="{Binding IsEnablePreviousPointer}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <Rectangle
                                                Width="10"
                                                Height="10"
                                                Fill="{DynamicResource AccentBaseColorBrush}">
                                                <Rectangle.OpacityMask>
                                                    <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_navigate_previous}" />
                                                </Rectangle.OpacityMask>
                                            </Rectangle>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>

                    <Image
                        Height="Auto"
                        MaxHeight="60"
                        Margin="5"
                        VerticalAlignment="Center"
                        Source="{Binding Comments.MediaPath}">
                        <Image.ContextMenu>
                            <ContextMenu>
                                <MenuItem Click="DeleteMedia_Click" Header="{DynamicResource LangKeyDelete}">
                                    <MenuItem.Icon>
                                        <Rectangle
                                            Width="15"
                                            Height="18"
                                            Margin="0,0,0,3"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Fill="Black">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_delete}" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </Image.ContextMenu>
                    </Image>
                    <StackPanel
                        x:Name="PART_MainStackPanel2"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Visibility="{Binding IsEnableNextPointer, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button
                            x:Name="PART_NavigateNextImage"
                            Margin="1,0,5,0"
                            VerticalAlignment="Center"
                            Background="{DynamicResource AccentBaseColorBrush}"
                            Click="PART_NavigateNextImage_Click"
                            Cursor="Hand"
                            Foreground="White"
                            IsEnabled="{Binding IsEnableNextPointer}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <Rectangle
                                                Width="10"
                                                Height="10"
                                                Fill="{DynamicResource AccentBaseColorBrush}">
                                                <Rectangle.OpacityMask>
                                                    <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_navigate_next}" />
                                                </Rectangle.OpacityMask>
                                            </Rectangle>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <Grid
                Grid.Column="1"
                Margin="10,30"
                HorizontalAlignment="Left"
                VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="{DynamicResource LangKeyQueries}" />
                <ComboBox
                    x:Name="ComboBoxQueries"
                    Grid.Row="0"
                    Grid.Column="1"
                    Width="200"
                    Margin="10"
                    x:FieldModifier="public"
                    ItemsSource="{Binding Comments.LstQueries, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox
                                x:Name="chkQuery"
                                Checked="chkQuery_Checked"
                                IsChecked="{Binding IsContentSelected}"
                                Unchecked="chkQuery_Unchecked">
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0} [ {1} ]">
                                            <Binding Path="Content.QueryValue" />
                                            <Binding Path="Content.QueryType" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </CheckBox>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    Text="{DynamicResource LangKeyFilterText}" />
                <TextBox
                    x:Name="TextBoxFilter"
                    Grid.Row="1"
                    Grid.Column="1"
                    Width="200"
                    AcceptsReturn="True"
                    Text="{Binding Comments.FilterText}" />
            </Grid>

        </Grid>
        <Button
            x:Name="btnAddCommentToList"
            Width="200"
            Margin="10,30,0,0"
            x:FieldModifier="public"
            Background="{DynamicResource AccentBaseColorBrush}"
            Command="{Binding AddCommentsCommand}"
            CommandParameter="{Binding CommandParameter}"
            Content="{DynamicResource LangKeyAddCommentToList}"
            Foreground="White" />

    </StackPanel>
</UserControl>