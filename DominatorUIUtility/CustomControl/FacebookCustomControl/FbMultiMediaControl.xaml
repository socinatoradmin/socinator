<UserControl
    x:Class="DominatorUIUtility.CustomControl.FacebookCustomControl.FbMultiMediaControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Name="FbMultiMedia"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid
        Name="MainGrid"
        Width="600"
        HorizontalAlignment="Left">

        <StackPanel Orientation="Vertical">

            <Button
                x:Name="BtnPhotos"
                Width="200"
                Height="30"
                Margin="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="{DynamicResource AccentBaseColorBrush}"
                Click="BtnPhotos_OnClick"
                Foreground="White"
                Visibility="{Binding FbMultiMediaModel.IsAddImageVisibile, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle
                            Width="15"
                            Height="15"
                            Fill="White">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_image_export}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock Margin="6,0" Text="{DynamicResource LangKeyPhotoVideo}" />

                    </StackPanel>
                </Button.Content>
            </Button>
            <ScrollViewer
                Width="400"
                HorizontalAlignment="Left"
                BorderThickness="0"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Disabled">
                <ListView
                    Name="ListView"
                    BorderThickness="0"
                    ItemsSource="{Binding FbMultiMediaModel.MediaPaths}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Orientation="Horizontal"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>

                                    <MediaElement
                                        Grid.Row="1"
                                        Height="Auto"
                                        MaxHeight="{Binding MediaHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Margin="5"
                                        VerticalAlignment="Center"
                                        Source="{Binding MediaPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <MediaElement.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Click="DeleteMedia_Click" Header="{DynamicResource LangKeyDelete}">
                                                    <MenuItem.Icon>
                                                        <Rectangle
                                                            Width="15"
                                                            Height="18"
                                                            Margin="0,0,0,3"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            Fill="Black">
                                                            <Rectangle.OpacityMask>
                                                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_delete}" />
                                                            </Rectangle.OpacityMask>
                                                        </Rectangle>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                            </ContextMenu>
                                        </MediaElement.ContextMenu>
                                    </MediaElement>

                                    <Button
                                        Grid.Row="1"
                                        Width="{Binding FbMultiMediaModel.MultiMediaValueModel.CloseButtonHeight}"
                                        Height="{Binding FbMultiMediaModel.MultiMediaValueModel.CloseButtonHeight}"
                                        Margin="4"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        HorizontalContentAlignment="Right"
                                        Background="Transparent"
                                        Click="ButtonBase_OnClick"
                                        Foreground="Red"
                                        Opacity=".8"
                                        Style="{StaticResource CircleButton}">
                                        <Rectangle
                                            Width="15"
                                            Height="15"
                                            Margin="2,2"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Fill="White"
                                            Opacity=".7">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Visual="{StaticResource appbar_close}" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </Button>

                                </Grid>

                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>

        </StackPanel>
    </Grid>

</UserControl>