<UserControl x:Class="DominatorUIUtility.CustomControl.PostAreaBoxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls">
    <Grid Name="gridPostAreaBox">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox>
            <GroupBox.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{DynamicResource LangKeyCaptionText}" Grid.Column="0" FontWeight="SemiBold"
                               FontSize="13" Foreground="White" HorizontalAlignment="Left" />
                    <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right" Height="20">
                        <CheckBox Name="chkbox_spinText" Content="{DynamicResource LangKeySpintax}"
                                  FontWeight="SemiBold" FontSize="13" Foreground="White" VerticalAlignment="Center"
                                  IsChecked="{Binding IsSpinText}" Background="Transparent" />
                        <Button Width="50" Height="30" Background="Transparent" HorizontalContentAlignment="Center"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center" Name="btnClear">
                            <StackPanel Orientation="Horizontal">
                                <Rectangle Width="20" Height="20" Fill="White">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_reset}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                </Grid>
            </GroupBox.Header>
            <TextBox AcceptsReturn="True" TextWrapping="Wrap" Controls:TextBoxHelper.Watermark="What's on your mind?"
                     Name="txtPostData" Height="100" VerticalScrollBarVisibility="Auto" FontSize="13"
                     FontWeight="SemiBold" Text="{Binding TextMessage}" />
        </GroupBox>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.75*" />
                <ColumnDefinition Width="0.75*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical">
                <Button Name="btnLoadPhotoVideo"
                        Width="120" Margin="10,10,0,0"
                        Height="27"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="#003569"
                        BorderThickness="1"
                        Content="Photo"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Foreground="White" />
                <TextBlock Name="txtblk_PhotoVideoCount" Text="{Binding Path=MediaSourceCount}"
                           VerticalAlignment="Center" FontWeight="SemiBold" HorizontalAlignment="Center"
                           Margin="0,10,0,0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="1" Visibility="Hidden">
                <TextBlock Text="Check-In : " FontSize="13" FontWeight="SemiBold" VerticalAlignment="Top"
                           Margin="0,10,0,0" HorizontalAlignment="Center" />
                <TextBox FontSize="13" FontWeight="SemiBold" VerticalAlignment="Top" HorizontalAlignment="Center"
                         Width="100" Margin="7,10,11,0" Height="25" Text="{Binding Checkin}" Name="txtCheck" />


            </StackPanel>

        </Grid>

        <Grid Grid.Row="2">
            <StackPanel>
                <ListView ItemsSource="{Binding MediaList}" Name="lstview_Images">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding}" Width="50" Height="50">
                                    <Image.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Delete" Height="30" Name="menu_DeleteSingleImage"
                                                      VerticalContentAlignment="Center">
                                                <MenuItem.Icon>
                                                    <Image />
                                                </MenuItem.Icon>
                                            </MenuItem>

                                        </ContextMenu>
                                    </Image.ContextMenu>
                                </Image>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Line X1="0" Y1="0" X2="450" Y2="0" Stroke="Gray" Margin="0,10,0,10" StrokeThickness="2"
                      VerticalAlignment="Bottom" />
            </StackPanel>


        </Grid>
    </Grid>
</UserControl>