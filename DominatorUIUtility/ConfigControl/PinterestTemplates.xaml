<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:local="clr-namespace:DominatorUIUtility.ConfigControl">
    <ControlTemplate x:Key="PinterestControlTemplate">
        <GroupBox Background="Transparent" BorderThickness="0">
            <GroupBox.Header>
                <Border Background="{DynamicResource AccentBaseColorBrush}" CornerRadius="8,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Label
                            Grid.Column="0"
                            HorizontalAlignment="Left"
                            Content="{DynamicResource LangKeyConfiguration}"
                            FontSize="20"
                            Foreground="White" />
                        <Button
                            Grid.Column="1"
                            Margin="0,0,8,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Command="{Binding SaveCmd}"
                            Cursor="Hand"
                            Foreground="White">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border
                                        x:Name="SaveBtnBorder"
                                        Background="Green"
                                        CornerRadius="4">
                                        <StackPanel Margin="5,3" Orientation="Horizontal">
                                            <Rectangle
                                                Width="15"
                                                Height="15"
                                                Fill="White">
                                                <Rectangle.OpacityMask>
                                                    <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_save}" />
                                                </Rectangle.OpacityMask>
                                            </Rectangle>
                                            <AccessText Margin="6,0,0,0" Text="{DynamicResource LangKey_Save}" />
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="SaveBtnBorder" Property="Background" Value="LimeGreen" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>

                        </Button>
                    </Grid>
                </Border>
            </GroupBox.Header>
            <StackPanel Margin="10">
                <StackPanel DataContext="{Binding PinterestOtherConfigModel}" Orientation="Horizontal">
                    <CheckBox
                        Margin="5"
                        Content="{DynamicResource LangKeyScrapeDataUpto}"
                        IsChecked="{Binding IsScrapDataBeforeSendToPerformActivity}" />
                    <controls:NumericUpDown
                        Margin="5"
                        IsEnabled="{Binding IsScrapDataBeforeSendToPerformActivity}"
                        Minimum="0"
                        Value="{Binding PaginationCount}" />
                    <TextBlock Margin="5" Text="{DynamicResource LangKeyPaginationsBeforeSendItToPerformActivity}" />
                </StackPanel>
                <StackPanel
                    Margin="5"
                    DataContext="{Binding PinterestOtherConfigModel}"
                    Orientation="Horizontal">
                    <Label Margin="5" Content="{DynamicResource LangUseUniqueFeatureAs}" />
                    <RadioButton
                        Margin="5"
                        Content="{DynamicResource LangCampaignWise}"
                        IsChecked="{Binding IsCampainWiseUnique}" />
                    <RadioButton
                        Margin="5"
                        Content="{DynamicResource LangAccountWise}"
                        IsChecked="{Binding IsAccountWiseUnique}" />
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </ControlTemplate>
</ResourceDictionary>