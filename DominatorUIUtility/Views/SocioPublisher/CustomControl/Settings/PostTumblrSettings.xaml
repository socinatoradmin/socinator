<UserControl
    x:Class="DominatorUIUtility.Views.SocioPublisher.CustomControl.Settings.PostTumblrSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
    </UserControl.Resources>
    <Grid x:Name="MainGrid">
        <StackPanel Orientation="Vertical">
            <CheckBox
                Margin="2"
                Content="{DynamicResource LangKeyTagUsers}"
                IsChecked="{Binding IsTagUser}" />
            <TextBox
                Width="404"
                Height="60"
                Margin="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                mah:TextBoxHelper.Watermark="{DynamicResource LangKeyAddTagUsers}"
                AcceptsReturn="True"
                IsEnabled="{Binding IsTagUser}"
                Text="{Binding TagUserList}"
                TextWrapping="Wrap"
                Visibility="{Binding IsTagUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibility}}" />
        </StackPanel>
    </Grid>
</UserControl>