<UserControl
    x:Class="DominatorUIUtility.Views.SocioPublisher.CustomControl.Settings.PostAdvancedSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="0.8*" />
        </Grid.RowDefinitions>
        <TabControl
            x:Name="AdvancedPostSettings"
            Grid.Row="0"
            ItemContainerStyle="{StaticResource TabItemStyle}"
            TabStripPlacement="Top">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" />
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <UserControl Content="{Binding Content.Value}" />
                    </ScrollViewer>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StackPanel
            Grid.Row="1"
            Margin="0,5,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="ButtonSave"
                Height="30"
                Margin="0,0,10,10"
                x:FieldModifier="public"
                Background="Green"
                Foreground="White">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle
                            Width="15"
                            Height="15"
                            Fill="White">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_save}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock
                            Margin="6,0"
                            Foreground="White"
                            Text="{DynamicResource LangKeySave}" />
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button
                x:Name="ButtonCancel"
                Height="30"
                Margin="0,0,8,10"
                x:FieldModifier="public"
                Background="#ef1d1d">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle
                            Width="15"
                            Height="15"
                            Fill="White">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_cancel}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock
                            Margin="6,0"
                            Foreground="White"
                            Text="{DynamicResource LangKeyCancel}" />
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>