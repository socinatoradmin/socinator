<UserControl
    x:Class="DominatorUIUtility.Views.SocioPublisher.CustomControl.Settings.PostTwitterSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
    </UserControl.Resources>
    <Grid x:Name="MainGrid">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <CheckBox
                    Name="CheckBoxDeletePost"
                    Margin="2"
                    Content="{DynamicResource LangKeyDeleteThePostAfter}"
                    IsChecked="{Binding IsDeletePostAfterHours}" />
                <mah:NumericUpDown
                    Width="40"
                    Height="25"
                    Margin="2"
                    IsEnabled="{Binding IsDeletePostAfterHours}"
                    Minimum="0"
                    Value="{Binding DeletePostAfterHours}" />
                <TextBlock
                    Margin="5"
                    VerticalAlignment="Center"
                    Text="{DynamicResource LangKeyHours}" />
            </StackPanel>
            <StackPanel Orientation="Vertical">
                <CheckBox
                    Margin="2"
                    Content="{DynamicResource LangKeyMentionUsers}"
                    IsChecked="{Binding IsMentionUser}" />
                <TextBox
                    Width="404"
                    Height="60"
                    Margin="2"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    mah:TextBoxHelper.Watermark="{DynamicResource LangKeyAddMentionUsers}"
                    AcceptsReturn="True"
                    IsEnabled="{Binding IsMentionUser}"
                    Text="{Binding MentionUserList}"
                    TextWrapping="Wrap"
                    Visibility="{Binding IsMentionUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibility}}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>