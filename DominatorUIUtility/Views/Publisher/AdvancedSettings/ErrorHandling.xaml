<UserControl
    x:Class="DominatorUIUtility.Views.Publisher.AdvancedSettings.ErrorHandling"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid x:Name="MainGrid">
        <StackPanel Margin="5">
            <CheckBox
                Margin="5"
                VerticalAlignment="Center"
                Content="{DynamicResource LangKeyStopTheCampaignForSpecificErrorCodes}" />
            <StackPanel>
                <TextBlock
                    Margin="30,5"
                    VerticalAlignment="Center"
                    Text="{DynamicResource LangKeyErrorCodesToStopFor}" />
                <TextBox
                    Width="220"
                    Margin="30,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center" />
                <StackPanel Margin="30,5" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="{DynamicResource LangKeyStopAfter}" />
                    <controls:NumericUpDown
                        Margin="5,0"
                        VerticalAlignment="Center"
                        Minimum="0" />
                    <TextBlock VerticalAlignment="Center" Text="{DynamicResource LangKeyPostsWithSameError}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>