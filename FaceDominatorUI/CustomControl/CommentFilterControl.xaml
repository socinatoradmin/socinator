<UserControl x:Class="FaceDominatorUI.CustomControl.CommentFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:customControl1="clr-namespace:FaceDominatorUI.CustomControl"
             xmlns:customControl2="clr-namespace:DominatorUIUtility.CustomControl.FacebookCustomControl;assembly=DominatorUIUtility"
             FontFamily="{DynamicResource GabaritoSemiBold}"
             xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Name="MainGrid">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <CheckBox Name="ChckCommentFilter" Margin="5,5" Content="{DynamicResource LangKeyCommentAddedBetween}"
                          FontWeight="SemiBold" Foreground="{DynamicResource TextColorBrushAccordingTheme}"
                          IsChecked="{Binding CommentFilter.IsCommentDateBetweenChecked}" />
                <controls:NumericUpDown Minimum="0" Value="{Binding CommentFilter.CommentedBeforeDays.StartValue}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextColorBrushAccordingTheme}"
                                        IsEnabled="{Binding CommentFilter.IsCommentDateBetweenChecked}" />
                <Label Content="{DynamicResource LangKeyTo}" VerticalAlignment="Center" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextColorBrushAccordingTheme}" />
                <controls:NumericUpDown Minimum="0" Value="{Binding CommentFilter.CommentedBeforeDays.EndValue}"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextColorBrushAccordingTheme}"
                                        IsEnabled="{Binding CommentFilter.IsCommentDateBetweenChecked}" />
                <Label Content="{DynamicResource LangKeyDays}" VerticalAlignment="Center" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextColorBrushAccordingTheme}" />
            </StackPanel>

            <StackPanel Margin="0,10" Orientation="Vertical">
                <CheckBox Name="ChckFilterByCommentText" Margin="5,5"
                          Content="{DynamicResource LangKeyFilterByCommentText}" FontWeight="SemiBold"
                          Foreground="{DynamicResource TextColorBrushAccordingTheme}"
                          IsChecked="{Binding CommentFilter.IsCommentTextFilter}" />
                <customControl2:SelectOptionControl x:Name="FilterText"
                                                    SelectedInputOption="{DynamicResource LangKeySelectFriends}"
                                                    LstSelectedInput="{Binding CommentFilter.ListFilterText}"
                                                    InputText="{Binding CommentFilter.FilterText}"
                                                    IsSelectButtonVisible="False"
                                                    SelectedOptionDisplayName="{DynamicResource LangKeySelectFriends}"
                                                    Margin="35,0"
                                                    Visibility="{Binding CommentFilter.IsCommentTextFilter,Converter={StaticResource BooleanToVisibilityConverter}}" />

            </StackPanel>

            <StackPanel Margin="2" Orientation="Horizontal" HorizontalAlignment="Center"
                        Visibility="{Binding IsSaveCloseButtonVisisble,Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button x:Name="SaveButton" Foreground="White" Click="SaveButton_OnClick" x:FieldModifier="public"
                        Margin="10,5,0,0" Background="{DynamicResource AccentColorBrush}"
                        Height="25" HorizontalAlignment="Center" BorderThickness="1" FontSize="12"
                        Content="{DynamicResource LangKeySave}" />


            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>