<local:ShareConfigurationBase x:Class="LinkedDominatorUI.LDViews.Tools.Engage.ShareConfiguration"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                              xmlns:local="clr-namespace:LinkedDominatorUI.LDViews.Tools.Engage"
                              mc:Ignorable="d"
                              xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                              xmlns:DhCustomControl="clr-namespace:DominatorUIUtility.CustomControl;assembly=DominatorUIUtility"
                              xmlns:LdCustomControl="clr-namespace:LinkedDominatorUI.CustomControl"
                              xmlns:converters="clr-namespace:DominatorHouseCore.Converters;assembly=DominatorHouseCore"

                              x:Name="ShareConfigurationUserControl"
                              xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand,ElementName=ShareConfigurationUserControl}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Name="MainGrid">
        <StackPanel>
            <DhCustomControl:AccountGrowthModeHeader x:FieldModifier="public" Name="AccountGrowthHeader"
                                                     VerticalAlignment="Top"
                                                     VideoTutorialLink="{Binding VideoTutorialLink}"
                                                     SelectionChangedCommand="{Binding SelectionChangedCommand,ElementName=ShareConfigurationUserControl}"
                                                     SaveCommand="{Binding SaveConfigurationsCommand}" />
        </StackPanel>

        <Grid Margin="0,40,0,0" ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ScrollViewer>

                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <TextBlock Text="{DynamicResource LangKeyStatus}" FontWeight="Bold" VerticalAlignment="Center"
                                   Margin="10,0,10,0" />
                        <Controls:ToggleSwitch x:Name="TglStatus"
                                               IsChecked="{Binding ShareModel.IsAccountGrowthActive}"
                                               OnLabel="Active﻿" OffLabel="Stopped﻿"
                                               VerticalAlignment="Center"
                                               FontSize="12"
                                               Style="{StaticResource MahApps.Metro.Styles.ToggleSwitch.Win10}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction
                                        Command="{Binding StatusChangedCommand,ElementName=ShareConfigurationUserControl}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Controls:ToggleSwitch>
                    </StackPanel>

                    <Expander Header="{DynamicResource LangKeySearchQuery}" IsExpanded="True" Margin="5">
                        <DhCustomControl:SearchQueryControl IsExpanded="True"
                                                            Name="ShareSearchControl"
                                                            ListQueryType="{Binding ShareModel.ListQueryType}"
                                                            ListQueryInfo="{Binding ShareModel.SavedQueries}"
                                                            AddQueryCommand="{Binding AddQueryCommand}"
                                                            CommandParameter="{Binding ElementName=ShareConfigurationUserControl}"
                                                            CustomFilterCommand="{Binding CustomFilterCommand}"
                                                            DeleteQueryCommand="{Binding DeleteQueryCommand}"
                                                            DeleteMulipleCommand="{Binding DeleteMulipleCommand}" />
                    </Expander>

                    <StackPanel Orientation="Vertical">
                        <!-- After JobConfiguration  Expander-->
                        <Expander IsExpanded="True" Header="{DynamicResource LangKeyJobConfiguration}" Margin="5,5">
                            <DhCustomControl:JobConfigControl JobConfiguration="{Binding ShareModel.JobConfiguration}" />
                        </Expander>


                    </StackPanel>


                    <!-- After User Filters Expander-->

                    <Expander Header="{DynamicResource LangKeyUserFilters}" Margin="5,5" IsExpanded="True"
                              Visibility="{Binding ShareModel.IsEnableAdvancedUserMode,Converter={StaticResource BooleanToVisibilityConverter}}">
                        <LdCustomControl:UserFiltersControl x:Name="UserFiltersControl"
                                                            LDUserFilter="{Binding ShareModel.LDUserFilterModel}" />
                    </Expander>

                    <!--Manage Blacklist Users  expander-->

                    <Expander Header="{DynamicResource LangKeyManageBlacklistedUsers}" Margin="5,5"
                              Visibility="{Binding ShareModel.IsEnableAdvancedUserMode,Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <CheckBox Content="{DynamicResource LangKeySkipBlacklistedUsers}" Margin="10,5"
                                      IsChecked="{Binding ShareModel.IsChkSkipBlackListedUser}" />
                            <StackPanel Margin="25,5"
                                        Visibility="{Binding ShareModel.IsChkSkipBlackListedUser, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Orientation="Horizontal">
                                <CheckBox Content="{DynamicResource LangKeyPrivateBlacklist}" Margin="5,0"
                                          IsChecked="{Binding ShareModel.IsChkPrivateBlackList}" />
                                <CheckBox Content="{DynamicResource LangKeyGroupBlacklist}" Margin="5,0"
                                          IsChecked="{Binding ShareModel.IsChkGroupBlackList}" />
                            </StackPanel>
                        </StackPanel>
                    </Expander>

                </StackPanel>

            </ScrollViewer>

        </Grid>
    </Grid>
</local:ShareConfigurationBase>